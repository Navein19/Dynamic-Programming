class Solution:
    def integerBreak(self, n: int) -> int:
        
        if n==1:
            return 1
        
        
        dp=[[0 for j in range(n+1)] for i in range(n)]
        
        
        dp[1]=[1 for j in range(0,n+1)]
        dp[1][0]=0
        
        ))
